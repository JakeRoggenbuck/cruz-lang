~ Factorial function ~
fun factorial (int x) -> int {
        int counter = result = 1
        ~ Multiply result by everything from 1 to couinter ~
        while (counter++ < x) {
                result *= counter
        }
        return(result)
}
